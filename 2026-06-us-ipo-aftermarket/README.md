# 2026-06-us-ipo-aftermarket

⚠️ **`index.html` here is GENERATED — do not hand-edit it.**

It is a copy of `dist/index.html` built in the **`us-ipo-research`** repo
(the source of truth: data pipeline + dashboard UI). Editing it here will be
overwritten on the next deploy.

To change anything (UI, data, window) or to re-enable a password gate:

```bash
cd us-ipo-research
./deploy.sh            # rebuild + copy here + push (Cloudflare auto-redeploys)
```

See `us-ipo-research/MAINTAINING.md` for the full guide.
